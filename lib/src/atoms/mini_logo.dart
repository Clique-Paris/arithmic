import 'package:flutter/widgets.dart';

/// Widget contains the definition of the mini arithmic logo
class MiniLogo extends StatelessWidget {
  const MiniLogo({
    Key key,
  });
  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/logo.png');
  }
}
